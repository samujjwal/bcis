package physicalc;

/** TypeError is raised when an operation cannot be performed because
 * the types of its arguments are incompatible. */
class TypeError extends InterpreterError {
    public TypeError(java.lang.String operation, Datum object1, Datum object2) {
	// TODO
    }
}
